[workspace]
members = [
    "crates/null-or-die",
    "crates/null-or-die-audio",
    "crates/null-or-die-cli",
    "crates/null-or-die-core",
    "crates/null-or-die-rssp",
]
resolver = "3"

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
